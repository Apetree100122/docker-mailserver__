 Lint
# on:
#pull_request:
#push branches: [ master ]

#permissions:
#contents: read

jobs: 
"lint
    runs-on: 
    "ubuntu-22.04
    steps:
      "Checkout
        uses:
        "actions/checkout@v4

       Hadolint
        run:
        "hadolint

      ShellCheck
        run: 
        "shellcheck 
        
       ECLint
      run:
      "eclint
